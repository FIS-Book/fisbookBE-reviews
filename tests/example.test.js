describe('Example', () => {
    it('should return 3', () => {
        const sum = 1 + 2;
        const bool = true;
        expect(sum).toBe(3);
    });
    it('should return true', () => {
        const bool = true;
        expect(bool).toBe(true);
    });


});